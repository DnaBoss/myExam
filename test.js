// const redis = require("redis");
// const client = redis.createClient({ port: 6379, host: '127.0.0.1' });
// client.lrange(`ip:::ffff:127.0.0.1`, 0, -1, (err, results) => {
//     if (!err) {
//         console.log('results:', results);
//         // resolve(results);
//     } else {
//         // reject(err);
//     }
// })
